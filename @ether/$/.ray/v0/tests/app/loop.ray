// Control flow: `if` chains through `then`/`elsif`/`else`, and the loops take
// a predicate and a block, re-reading the predicate each time round.

flag ^keyword := true

while flag { flag = false }

seen ^keyword := false

do { seen = true } while flag

picked := (if flag then (seen) else (flag))()
