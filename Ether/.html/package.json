{
  "scripts": {
    "start": "npx vite & cd electron && npm start & wait",
    "build": "npx vite build && cd electron && npm run build",
    "install": "cd electron && npm install"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  }
}
