#!/usr/bin/env bash
set -euo pipefail
# EditorConfig is a configuration format. No installation needed.
# Optionally install editorconfig-checker for validation.
echo "EditorConfig is a configuration format. No installation required."
